#include "RoutePlanner.hpp"


std::vector<Point> RoutePlanner::calcPath( const Board & board, const Point & start, const Point & goal )
{
    // TODO

    std::vector<Point>path;

    return std::move(path);
}